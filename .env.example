# ═══════════════════════════════════════════════════════════
#  NetScan Pro — Environment Configuration
#  HOW TO USE:
#    1.  cp .env.example .env
#    2.  Fill in the values below (especially JWT_SECRET)
#    3.  Never commit .env to git
# ═══════════════════════════════════════════════════════════

# ── Server
PORT=5000
NODE_ENV=development

# ── JWT  (change this to a long random string in production!)
JWT_SECRET=mysecretkey123456789abcdef
JWT_EXPIRES_IN=7d

# ── CORS  (comma-separated list of allowed front-end origins)
#    If you open HTML directly in browser, add file:// or use a live server
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000,http://localhost:8080

# ── Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=200

# ── In-memory limits
MAX_HISTORY_PER_USER=100
