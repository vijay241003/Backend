# ═══════════════════════════════════════════════════════
#  NetScan Pro Backend — Environment Variables
#  1. Copy this to .env
#  2. Fill in MONGODB_URI from MongoDB Atlas
#  3. Change JWT_SECRET to something long and random
# ═══════════════════════════════════════════════════════

# Server
PORT=5000
NODE_ENV=production

# MongoDB Atlas connection string
# Get this from: Atlas → Your Cluster → Connect → Drivers
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/netscan?retryWrites=true&w=majority

# JWT — CHANGE THIS to a long random string!
JWT_SECRET=change_this_to_a_long_random_string_minimum_32_chars
JWT_EXPIRES_IN=7d

# CORS — your Netlify frontend URL (set after deploying frontend)
CORS_ORIGINS=https://your-app.netlify.app,http://localhost:5500,http://127.0.0.1:5500
